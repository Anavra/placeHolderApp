<resources>

    <!-- Base application theme. -->
    <style name="PlaceHolderTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="colorPrimaryDark">@color/color_primary_variant</item>
        <item name="colorPrimary">@color/color_primary_variant</item>

        <item name="android:background">@color/color_primary_variant</item>

        <!-- Text styles -->
        <item name="android:textAppearance">@style/TextAppearance</item>
        <item name="android:textColorPrimary">@color/color_on_primary_variant</item>

        <item name="android:editTextColor">@color/color_on_primary_variant</item>
        <item name="android:editTextBackground">@color/color_background</item>

        <!-- Button styles -->
        <item name="buttonStyle">@style/MainButton</item>
        <item name="colorButtonNormal">@color/secondary_button</item>
        <item name="android:buttonCornerRadius">4dp</item>

    </style>

    <style name="TitleTextAppearance" parent ="TextAppearance.AppCompat.Body1">
        <item name="android:textSize">30sp</item>
    </style>

    <style name="TextAppearance">
        <item name="android:textColor">@color/color_on_primary_variant</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textStyle">normal</item>
    </style>


    <style name="DebugTextAppearance">
        <item name="android:fontFamily">monospace</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textColor">@color/color_error</item>
    </style>

    <style name="MainButton" parent="Widget.AppCompat.Button.Colored">
        <item name="buttonStyle">@style/Widget.AppCompat.Button</item>
        <item name="background">@color/color_on_primary</item>
        <item name="colorButtonNormal">@color/secondary_button</item>
    </style>

    <style name="SecondaryButton" parent="Widget.AppCompat.Button">
        <item name="buttonStyle">@style/Widget.AppCompat.Button</item>
    </style>

</resources>
